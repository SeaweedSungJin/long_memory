# V19 실제 체크포인트 진단 — 2026-09-20

## 실행 범위와 결론

기존 validation 전체를 재분석하고, 최종 prefix 체크포인트의 원본 RGB 재생·수치 정렬·bank 제거 검사를 실행했다. 대규모 정책 재학습이나 새 simulator rollout은 하지 않았다. 추가 학습은 정책을 동결한 소규모 subgoal 진단 decoder뿐이다.

**현재 근거로는 “메모리 코드가 동작하지 않는다”보다 “학습한 메모리 경로가 실제 생성 행동의 오차를 안정적으로 줄이지 않는다”가 정확하다.** 학습/추론 정밀도 차이도 실제로 확인됐지만, 그것이 낮은 성공률의 원인이라는 증거는 아직 없다.

기존 학습·평가·캐시·체크포인트를 수정하지 않았다. 추가한 것은 `diagnose_v19_*`, 결과 요약 CLI, 테스트, 이 문서다. `.gitignore`에는 `*logs*`에 가려지던 새 진단 소스/테스트 두 파일만 예외 처리했다. 원래 학습·추론 파일은 변경하지 않았다.

## 1. 어떤 실행을 검사했는가

- 시작 HEAD: `009ef0cda240901c8d252c89cb96ac312b9e98e6`; 시작 시 worktree clean.
- 실제 학습 provenance의 소스 파일 148개를 현재 파일과 대조: 변경 없음.
- 학습: `runs/long_memory/v19_fullcoverage_v1/{control_full,prefix_full}`.
- 두 arm 모두 최종 `checkpoint-006072`, 1 epoch, 24,286 cached TRAIN query를 처리하고 `status=complete`.
- control의 tail weight=1, prefix의 tail weight=0.25. 구조/초기화/학습 query 계획/예산은 동일하다.
- 캐시: `runs/long_memory/cache_full1600_v1`; fingerprint `f4ee3fabe5d0e41840a72b8345d2688a94e94a4296689dea98adc284935ccb46`.
- 원본 데이터: `/home/sjkim/HAMLET-Isaac-GR00T_backup/data/robomme`.
- 원본 HAMLET: `checkpoints/author_hamlet_robomme/checkpoint-60000`.
- V19 표현: HAMLET short 4×2048 → 저장 encoder 4×256 → FIFO32 events → cross-attention reader → linear gate/residual fusion → LoRA-adapted AE. HAMLET short 자체는 동결했다.
- 1 epoch는 **모든 cached TRAIN endpoint**를 사용했다는 뜻이다. 모든 RGB 프레임이나 held-out VAL까지 학습했다는 뜻은 아니다.

체크포인트/계획/validation의 실제 SHA256 및 설정은 [로그 보고서 JSON](runs/diagnostics/v19_20260920/logs/report.json)에 있다. 새 GPU 진단도 checkpoint sidecar와 입력 manifest/validation의 전후 hash가 같음을 검사했다. 원본 대형 HAMLET shard 식별은 기존 provenance의 JSON hash+파일 size/mtime 방식이며, 새로 모든 base shard를 내용 hash한 것은 아니다.

## 2. 기존 로그 재분석

이미 저장된 reader/memory-off/baseline 3-way validation을 사용했다. 두 arm × 8단계(0, 1000, …, 6000, 6072), 단계마다 16 task × 4 episode/query × 2 noise이다. noise 반복을 먼저 query별로 평균하고, task 안에서 **episode 단위 paired bootstrap 2,000회**를 수행했다. 인접 프레임/두 noise를 독립 episode로 세지 않았다.

부호: `memory gain = off - reader`, `AE gain = baseline - off`, `total gain = baseline - reader`. 모두 양수가 오차 감소다. `memory-off`는 같은 공동학습 체크포인트의 READ를 끈 것이지, 별도로 학습한 AE-only가 아니다.

최종 prefix의 공통 unweighted 지표:

| 지표 | baseline | memory-off | reader |
|---|---:|---:|---:|
| 원래 action/flow loss | 0.01063720 | 0.00988831 | 0.00852143 |
| 생성 행동의 관측된 실행 prefix MSE | 0.00681486 | 0.00669242 | 0.00604411 |
| 생성 행동의 관측된 실행 prefix MAE | 0.01287735 | 0.01320994 | 0.01297072 |
| joint7 생성 MAE | 0.01056921 | 0.01089053 | 0.01091816 |

실행 prefix는 nominal horizon 전체가 아니라 기존 `action_mask`로 실제 관측된 prefix 좌표를 제한한 것이다. 값은 checkpoint의 정규화 action 좌표이며 물리 단위/성공률이 아니다.

- prefix memory MSE gain: **+0.00064831**, 95% CI **[−0.00019140, +0.00209665]**.
- MoveCube episode 917 / decision 30 한 query가 이 net gain의 **111.48%**를 설명한다. 해당 query를 빼고 task weight를 다시 맞추면 **−0.00007421**로 뒤집힌다. 두 noise를 두 사례로 세지 않았다.
- distinct query 기준 memory MSE 개선/악화/동일 = **31/32/1**.
- prefix의 memory MAE gain은 +0.00023921, CI [+0.00000984, +0.00050330]이지만, **baseline 대비 total MAE gain은 −0.00009338**이며 CI는 0을 포함한다. 메모리 ON이 공동학습 AE의 일부 악화를 상쇄한 것과 원본보다 좋아진 것은 다르다.
- joint와 gripper를 나누면, 평균 MSE 감소는 대부분 gripper 쪽이다. joint 생성 MAE는 baseline보다 낮아지지 않았다.
- control 역시 teacher-conditioned flow 개선을 실제 generated 행동/성공률 개선으로 연결하지 못했다.

모든 단계/task/role 수치와 gain/CI: [stage_metrics.csv](runs/diagnostics/v19_20260920/logs/stage_metrics.csv), [stage_gains.csv](runs/diagnostics/v19_20260920/logs/stage_gains.csv). 영향이 큰 query와 제외 분석: [top_gain_queries.csv](runs/diagnostics/v19_20260920/logs/top_gain_queries.csv), [leave_out.csv](runs/diagnostics/v19_20260920/logs/leave_out.csv).

기존 rollout도 재학습/재실행 없이 호환성 검증 후 재집계했다:

| 기존 VAL160 rollout | 성공 |
|---|---:|
| HAMLET | 38/160 = 23.750% |
| control | 35/160 = 21.875% |
| prefix | 37/160 = 23.125% |

prefix−HAMLET = −0.625pp, paired CI [−5.00, +3.75]pp. 개선도 악화도 확정할 수 없다. 반복 개발용 benchmark VAL160과 위 offline cache-VAL은 서로 다른 평가 대상이다. 둘을 같은 episode로 대응시키지 않았다.

## 3. 관측 → 저장 → fusion 수치 검사

최종 prefix 체크포인트와 VideoPlaceOrder의 고정 cache-VAL episode 1355를 사용했다. 같은 원본 RGB/현재 state/언어/81개 canonical endpoint를 다음 경로에 통과시켰다:

1. 기존 저장 캐시.
2. 원래 `cache.py::_extract_episode`를 그대로 재실행: 새 캐시는 저장하지 않음.
3. 실제 `RepresentationPolicyV18._get_action`의 현재 기본 경로.
4. 같은 policy를 BF16 autocast로 감싼 진단 조건.
5. 4에 더해 캐시와 같은 post-feature BF16 양자화를 적용한 **진단 조건**.
6. 3을 같은 session ID로 reset 후 반복.

여기서는 중복 denoising만 임시 stub으로 생략했다. backbone, 원래 HAMLET short cache, memory encoder/read/write/fusion, session/reset/cadence 코드는 실제 경로다. 별도의 행동 오차 검사에서는 stub을 제거하고 기존 flow/Euler 생성 함수를 그대로 사용했다. 실제 online rollout이나 정책이 만든 상태열의 검사는 아니다.

| 비교 | 측정 결과 |
|---|---|
| 재생성 cache vs 기존 cache | moment/short/전체 backbone 특징/state/encoded/bank/fused가 일치 |
| 기본 online vs cache | short 상대 L2 평균 약 1.02%, 최대 3.89%; encoded 최대 5.53% |
| 기본 online 반복/reset | 검사 tensor 81 endpoint 모두 반복 일치 |
| online BF16 autocast만 적용 | short 최대 상대 L2 약 0.181%; moment를 양쪽 BF16으로 비교하면 일치 |
| autocast + 캐시 BF16 저장 정밀도 | 전체 특징/encoded/bank/fused **81/81 정확히 일치**; moment/short도 수치 차이 0, 표시 dtype만 다름 |
| 저장 cache 입력의 batch encode vs 순차 step | 최대 상대 L2: encoded 1.53e−7, fused 6.48e−8 |

마지막 행은 GPU 연산의 batch shape가 다른 비교다. 이를 reset/인덱싱 실패로 해석하지 않았다. 위의 bank/fused 정확 일치는 같은 cache 입력을 순차 재생한 기준과의 비교다.

episode 1355 / decision 67에서 두 동일 noise로 실제 AE를 실행한 결과:

| 특징 경로 | generated-prefix MSE | MAE |
|---|---:|---:|
| 저장 cache | 0.000315499 | 0.0108901 |
| 기본 online | 0.000309431 | 0.0107085 |
| cache-aligned online 진단 | 0.000315499 | 0.0108901 |

**정밀도 차이는 실제로 존재하고, 정렬하면 캐시와 행동 오차까지 재현된다. 하지만 이 한 query에서는 기본 online 오차가 오히려 조금 낮다. 따라서 “정밀도 불일치를 고치면 성공률이 오른다”는 결론은 아직 불가하다.**

원본 rollout client의 `demo_endpoints()`도 실제 호출해 cache와 대조했다. demo cadence가 일치하고, execution은 n_demo부터 stride16이다. reset 후 이전 bank 오염도 재현되지 않았다. 마지막 cache endpoint는 terminal 상태 기록용으로 온라인이 이미 종료했을 수 있으므로, 그 위치에는 행동 오차를 계산하지 않았다.

episode 1355의 demo는 1,059 raw frames지만 마지막으로 관측한 demo frame은 1,043이다. 1,044–1,058은 canonical sampling에 포함되지 않는다. 이건 현재 cache/online 간 차이가 아니라 **두 경로가 공유하는 sampling 한계**다. 그 15프레임에 필요한 의미가 있는지는 이번 검사로 알 수 없다.

자세한 수치: [parity.csv](runs/diagnostics/v19_20260920/runtime_prefix_quantization/parity.csv), [parity_action_errors.csv](runs/diagnostics/v19_20260920/runtime_prefix_quantization/parity_action_errors.csv), [설정과 식별 정보](runs/diagnostics/v19_20260920/runtime_prefix_quantization/configuration.json).

## 4. 같은 short를 고정한 bank 개입

기존 validation schedule에서 VideoPlaceOrder 4 episodes(1355, 1300, 1309, 1336), query 각 1개, noise 각 2개를 그대로 사용했다. 현재 short/query, 현재 state, GT/mask, flow noise/timestep seed, 생성 seed는 조건 간 동일하다.

정상 bank/동일 AE의 memory-off/4개 연속 시간 블록 제거/같은 크기의 random event set 제거를 실행했다. 이어 요청의 random **block** 대조도 명확히 충족하도록 같은 크기의 임의 연속 구간 제거를 추가했다. 최종 14조건 × 4 episodes × 2 noise = **112개 측정**을 완료했다. 기존 random-set 결과는 그대로 보존한다. K/V 동시 row shuffle은 사용하지 않았다.

첫 검사에서:

- memory-off−normal generated-prefix MAE = **−0.00014606**, episode bootstrap CI [−0.00029185, −0.00005290]. 네 episode 모두 메모리 OFF가 낮았다.
- MSE 차이는 −0.00000352, CI는 0을 포함했다.
- 시간 블록 제거 효과는 크기/방향이 일정하지 않았고, 특정 과거 블록을 올바르게 검색한다는 근거를 확보하지 못했다.
- 추가한 contiguous random-block과 각 시간 블록을 비교한 generated-prefix MSE 차이도 4개 비교 모두 episode CI가 0을 포함했다.
- 이는 “memory 입력이 행동에 아무 영향도 없다”와 다르다. 개입하면 실제 generated action이 바뀌지만 그 변화가 GT에 더 가까워지는 방향으로 안정적이지 않았다.

FIFO 출처 기록 예시: episode 1355의 첫 실행 query 67 직전 bank에는 events **35–66**만 남는다. 네 토큰짜리 32개 event 모두 demo이고, 그 HAMLET source window들의 관측 frame 범위는 **499–1043**이다. 시작 부분의 과거 source는 이 bank에 남아 있지 않는다. 따라서 “FIFO32가 demonstration 전체를 보존한다”는 전제는 이 사례에서 틀리다. 그렇다고 사라진 부분이 반드시 성공에 필요했다는 뜻은 아니다.

각 event의 episode/event ID, endpoint/source frames, demo 여부, 삽입/삭제 시점, 현재 bank 위치는 [bank_provenance.json](runs/diagnostics/v19_20260920/runtime_prefix_quantization/bank_provenance.json)에 있다. 토큰 하나는 프레임/물체 하나가 아니다. Q=4 토큰은 K=4 source window를 섞은 표현이다. 제거한 event의 source 중 일부는 이웃 event나 현재 short에 중복으로 남는다.

최종 개입/정밀도 통합 [보고서](runs/diagnostics/v19_20260920/runtime_summary_blocks/report.md), [episode-paired 수치 CSV](runs/diagnostics/v19_20260920/runtime_summary_blocks/intervention_summary.csv), [112개 원시 측정](runs/diagnostics/v19_20260920/runtime_prefix_blocks/interventions.csv), [중복 source 기록](runs/diagnostics/v19_20260920/runtime_summary_blocks/removal_source_overlap.csv). 정밀도 실행은 기존 결과를 재사용하고 최종 block 검사는 `--skip-parity`로 실행했다.

## 5. 조건부 subgoal probe

앞 단계만으로 encoder/retrieval/fusion을 구별할 수 없어 PatternLock의 작은 frozen-policy decoder 검사를 **실행 완료했다**. 기존 V15/V16 weak temporal retrieval의 NO-GO 기록을 먼저 검토했으며, 이번에는 Q/K나 policy를 학습하지 않고 각 단계 표현에서 현재 subgoal을 읽는 decoder만 학습했다.

- TRAIN 32 episodes/248 queries, 기존 cache-VAL 16 episodes/97 queries. TEST 미사용. 선택 seed 9191/9192, 라벨을 보지 않고 episode를 선택했다.
- raw `simple_subgoal_online[frames[decision]]`를 현재 방향 8-class target으로 사용했다. 일부 16-step chunk 중간에는 라벨이 바뀔 수 있으므로 chunk 전체 라벨로 해석하지 않았다.
- `simple_subgoal`, `simple_subgoal_online`, `grounded_subgoal`, `grounded_subgoal_online`, `is_demo`, `episode_index`, `frame_index`가 실제로 있다. `is_subgoal_boundary`, `choice_action`은 없다.
- 동일한 27,592-parameter attention+MLP decoder, 300 updates, 2 seeds, 동일 TRAIN batch schedule, final-step 결과만 보고했다. VAL best selection 없음.
- feature 폭 차이를 맞추기 위해 **학습하지 않는 고정 random projection**을 썼다. P0/P5는 같은 projection, P2/P3/P4도 같은 projection이다. 이 추가 병목과 작은 decoder의 한계 때문에 실패를 정보 부재로 단정할 수 없다.
- P0/P5에는 명시적인 시간값을 넣지 않았다. Ptime은 현재 시점까지 알 수 있는 clock만 사용하며 episode 총길이/미래 진행률은 넣지 않았다. 과거 token 순서/관측 시점은 역사 입력에만 보존한다. 라벨/GT action은 feature extraction에 전달하지 않았다.

| 입력 | TRAIN | cache-VAL |
|---|---:|---:|
| P0: 현재 short + state | 69.45% | 27.88% |
| Ptime: 시간/경과 정보만 | 31.69% | 18.47% |
| P1: P0 + 모든 과거 short/state/time | 89.08% | 31.44% |
| P2: P0 + eviction 전 encoder 출력 | 61.71% | 26.81% |
| P3: P0 + 실제 FIFO | 65.02% | 25.74% |
| P4: P0 + 실제 attention READ 출력 | 51.68% | 18.08% |
| P5: fused short + state | 74.38% | 26.68% |

수치는 두 고정 decoder seed의 **episode-macro subgoal 분류 정확도**다. 로봇 task 성공률이 아니다.

- P1−P0 = +3.56pp, episode-bootstrap CI [−4.89, +12.44]pp.
- P1−Ptime = +12.97pp, CI [+1.70, +25.47]pp. 하지만 P0보다 낫다는 근거는 약하므로 장기기억 필요성까지 증명한 것은 아니다.
- P5−P0 = −1.20pp, CI [−7.22, +5.04]pp.
- P2−P1, P4−P3 등 인접 단계의 paired CI도 모두 0을 포함한다. 저장 encoder/reader 쪽의 점추정은 낮지만 **병목 위치를 통계적으로 특정하지 못했다**.
- PatternLock의 이 VAL에는 32개를 초과하는 과거 event가 없어 **P2/P3 입력이 모든 held-out query에서 동일**하다. 서로 다른 학습 입력/정규화/decoder 때문에 예측값은 다를 수 있으나, 그 차이를 VAL에서의 eviction 손실이라고 하면 안 된다. TRAIN에만 eviction query 3개가 있었다.
- P1의 TRAIN 89.08%와 VAL 31.44% 차이는 이 작은 probe의 일반화 한계를 보여준다. 원래 V19 정책 전체가 같은 방식으로 과적합했다는 직접 증거는 아니다.

정책 hash 전후 동일, policy gradient 없음, CUDA 미초기화. 실행 9.04초. [결과](runs/diagnostics/v19_20260920/subgoal_prefix/summary.json), [프로토콜](runs/diagnostics/v19_20260920/subgoal_prefix/protocol.json), [라벨 정렬/episode 목록](runs/diagnostics/v19_20260920/subgoal_prefix/target_plan.json), [상태·무결성](runs/diagnostics/v19_20260920/subgoal_prefix/status.json).

## 6. 병목 판정과 다음에 바꿀 변수 하나

| 조사 대상 | 현재 판정 |
|---|---|
| 관측 sampling/캐시·추론 | **정밀도 불일치는 확정**. cadence/reset은 검사 범위에서 일치. 마지막 demo 15프레임 누락도 확정하나 의미상 피해는 모름 |
| HAMLET short | 모든 과거 short가 현재만보다 낫다는 점추정은 있으나 CI가 넓음. short 표현을 폐기할 근거 없음 |
| 저장 encoder | P1→P2 점추정 하락은 후보. projection/decoder/표본 한계 때문에 확정 불가 |
| FIFO 범위 | VideoPlaceOrder 사례에서 demo 앞부분이 보존되지 않음은 확정. 그 내용이 성공에 필요한지는 미확인 |
| retrieval | P4 점추정 및 block 검사상 유용한 검색을 입증 못함. 원인이라고 특정하지도 못함 |
| fusion | P5가 P0보다 개선되지 않음. 검색/AE 효과와 분리한 fusion 원인 판정은 불가 |
| AE 활용 | 메모리 개입에 생성 행동이 실제 변하므로 경로가 완전히 무시되지는 않음. 그 변화가 도움이 된다는 근거는 약함 |

**다음 변수는 모델 크기/새 writer가 아니라 `inference feature precision contract: native ↔ cache-aligned` 하나를 권한다.** 이번 진단에는 cache-aligned 조건을 넣었지만 배포 policy는 아직 수정하지 않았다. 확인된 입력 불일치를 제거한 기준을 먼저 만드는 목적이며 성능 향상을 약속하는 제안은 아니다.

1. 동일 checkpoint/관측 cadence/RGB/state/GT/noise/AE를 유지하고, VLM→short→외부 memory 경계의 autocast+BF16 저장 정밀도 규칙만 바꾼다. AE denoising의 정밀도/step 수는 바꾸지 않는다.
2. **구현 성공 기준:** 원본 캐시를 재현하는 작은 고정 episode 패널에서 normalized moment를 같은 dtype으로 비교했을 때와 전체 특징/short/bank/fused의 오차가 0 또는 기존 batch-shape 반복 오차 범위로 줄고, reset/cadence/마스크가 변하지 않아야 한다. 이 조건은 episode 1355에서 이미 충족했다.
3. **성능 가설의 성공 기준:** 이후 동일 checkpoint의 native/aligned를 기존 고정 VAL160 scenario/seed로 paired 비교한다. 점수 하나가 아니라 wins/losses와 episode CI를 보고, CI가 0을 넘는 양의 개선이 있어야 성능 원인 근거로 삼는다. 0을 포함하면 미확정, 의미 있는 악화면 개선 가설을 기각한다. 원본 baseline/다른 arm을 새 정밀도로 실행했다면 기존 native baseline 기록과 섞지 않고 별도 provenance로 남긴다.
4. **개선이 없으면:** 정밀도 불일치는 실제 engineering 차이였지만 주된 성능 원인은 아니었다고 결론 내린다. 그 뒤 encoder/FIFO/reader 중 하나만 조사한다. 이번 probe의 순위만 보고 세 모듈을 동시에 바꾸지 않는다.

이외에 학습 objective를 새로 추가하거나 CVoM/writer를 변경하지 않았다. 시간 블록/동일 크기 random 제거로 충분히 제한된 증거를 얻었으므로 다른 history로 content를 교체하는 OOD 실험과 수동 정답 frame 지정은 하지 않았다. 의미를 판정할 근거가 없어 RGB 썸네일로 검색의 정답을 주장하지도 않았다.

여전히 모르는 점: 실제 simulator 렌더링/online state 분포에서 같은 정밀도 변화가 어떤 영향을 주는지, evicted demo 앞부분의 의미가 정말 필요한지, 긴 task의 의미가 HAMLET short에 얼마나 보존되는지, 관측/저장/검색/제어 중 무엇을 바꿔야 성공률이 오르는지다. 한 task의 수치 재현과 subgoal probe만으로 이를 확정하지 않았다.

## 7. 재실행

아래는 동일 프로토콜 재실행용이다. 결과 디렉터리가 이미 있으면 덮어쓰지 않고 중단한다. 필요하면 `v19_recheck`를 새 이름으로 바꾼다. GPU CLI는 다른 CUDA compute 작업이 감지되면 모델을 로드하지 않는다.

```bash
cd /home/sjkim/HAMLET-Isaac-GR00T

.venv/bin/python run_scripts/robomme/diagnose_v19_logs.py \
  --output-dir runs/diagnostics/v19_recheck/logs

CUDA_VISIBLE_DEVICES=0 OMP_NUM_THREADS=4 MKL_NUM_THREADS=4 \
.venv/bin/python run_scripts/robomme/diagnose_v19_runtime.py \
  --cache-dir runs/long_memory/cache_full1600_v1 \
  --checkpoint runs/long_memory/v19_fullcoverage_v1/prefix_full/checkpoint-006072 \
  --validation runs/long_memory/v19_fullcoverage_v1/prefix_full/validation-006072.json \
  --task VideoPlaceOrder --episodes 4 \
  --output-dir runs/diagnostics/v19_recheck/runtime

.venv/bin/python run_scripts/robomme/summarize_v19_diagnostics.py \
  --runtime-dir runs/diagnostics/v19_recheck/runtime \
  --output-dir runs/diagnostics/v19_recheck/runtime_summary

CUDA_VISIBLE_DEVICES= OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 \
.venv/bin/python run_scripts/robomme/diagnose_v19_subgoal.py \
  --cache-dir runs/long_memory/cache_full1600_v1 \
  --checkpoint runs/long_memory/v19_fullcoverage_v1/prefix_full/checkpoint-006072 \
  --steps 300 --threads 2 \
  --output-dir runs/diagnostics/v19_recheck/subgoal
```

새 policy training/writer/CVoM/policy loss는 포함하지 않는다. CPU 진단 decoder는 배포 모델에 부착하지 않는다.

회귀 테스트 실행:

실제 결과: **72개 테스트 통과**. 종료 후 CUDA compute 프로세스가 남지 않았고, 원래 학습 소스 148개는 provenance와 동일했다.

```bash
OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 NO_ALBUMENTATIONS_UPDATE=1 \
.venv/bin/python -m unittest \
  tests.test_diagnose_v19_logs tests.test_diagnose_v19_memory \
  tests.test_diagnose_v19_runtime tests.test_summarize_v19_diagnostics \
  tests.test_diagnose_v19_subgoal tests.test_flow_objective_v19 \
  tests.test_validation_v19 tests.test_training_plan_v19 \
  tests.test_full_training_workflow_v19 tests.test_full_memory_training_v19 -q
```
